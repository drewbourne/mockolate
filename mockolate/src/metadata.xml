<?xml version="1.0" encoding="utf-8" ?>
<annotations version="1.0">
    <metadata name="Mock" description="Annotates a field for which a Mock Object will be prepared and injected.">
        <context name="variable" />
        <attribute  name="type"
                    type="String" 
                    required="false" 
                    values="nice,strict"
                    defaultValue="nice"
                    description="Type of Mock Object to inject, either 'nice' or 'strict'."
                    />
        <attribute  name="inject"
                    type="Boolean" 
                    hint="boolean"
                    required="false"
                    defaultValue="true" 
                    description="Indicates if a Mock instance should be injected to the field."
                    />
    </metadata>
</annotations>