package mockolate
{
    import asx.array.filter;
    import asx.array.reject;
    import asx.object.isA;
    
    import flash.events.EventDispatcher;
    import flash.events.IEventDispatcher;
    
    import mockolate.ingredients.MockolatierMaster;
    
	[Deprecated]
    /**
	 * @private
     */
    public function preparedClassFor(classReference:Class, namespacesToProxy:Array = null):Class
    {
		return null;
//        return MockolatierMaster.preparedClassFor(classReference, namespacesToProxy);
    }
}
